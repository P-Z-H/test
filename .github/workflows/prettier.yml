name: Prettier

on: [push]

jobs:
  build:
      name: Prettier
      runs-on: ubuntu-latest

      steps:
      - uses: actions/checkout@v1

      - uses: npm -g -install

      - run: npm run format
      - run: git status



